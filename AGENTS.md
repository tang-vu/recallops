# Repository workflow

- After each completed update, run the checks appropriate to the change, commit it, and push to the current branch's remote.
- The user has authorized this workflow; do not ask again for routine commits and pushes.
- Exclude credentials, runtime databases, logs, and unrelated changes from commits. Never force-push without explicit authorization.
- Report the commit and push result when handing back an update.
