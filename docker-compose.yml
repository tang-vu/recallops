services:
  control-plane:
    profiles: ["future"]
    image: python:3.12-slim
    command: ["python", "-c", "print('Control-plane image is added after the vertical slice')"]

  web:
    profiles: ["future"]
    image: node:24-alpine
    command: ["node", "-e", "console.log('Web image is added with the product interface')"]
